class DataCorrectionAugust18 < ActiveRecord::Migration[5.2]
  def change

    # translations =Translation.all
    # temp_hash = {}

    # translations.each do |translation|

    #   # temp_hash[t.id] = {} unless temp_hash.has_key?(t.id)
    #   # temp_hash[t.id]["english_phrase"] = t.input_phrase if t.input_language == "ENGLISH"
    #   # temp_hash[t.id]["arabic_phrase"] = t.input_phrase if t.input_language == "ARABIC"
    #   # temp_hash[t.id]["french_phrase"] = t.input_phrase if t.input_language == "FRENCH"
      
    # end

  end
end
