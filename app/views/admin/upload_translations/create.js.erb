
  // Showing Growl Like Message
  <% if @notification[:success] %>
    $("#pleaseWaitDialog").modal("hide");
    notifySuccess("<%= escape_javascript(@notification[:title]) %>", "<%= escape_javascript(@notification[:message]) %>");
    $("#btn").attr("disabled", false);  
  <% else %>
    $("#pleaseWaitDialog").modal("hide");
    $("#file").val(null);
    notifyError("<%= escape_javascript(@notification[:title]) %>", "<%= escape_javascript(@notification[:message]) %>");
  <% end %>
  
  <% if @summary.any? %>
   window.location.href = "<%= admin_translation_uploads_history_path(@history.id) %>"

  //  $("#FIRST_BOX").html("<%= escape_javascript(render(:partial=>'admin/upload_translations/index/summary.html.erb')) %>");
    $("#pleaseWaitDialog").modal("hide");
    $(document).ready(function() {
    $("#btn").attr("disabled", false);  
    });
  <% end %>

  // Load same index UI
  //$("#upload_translation_content").html("<%#= escape_javascript(render(:partial=>'admin/upload_translations/index/index.html.erb')) %>");
  
