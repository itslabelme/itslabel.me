// Partial template to load JS code for Data table and other general functionalities
var $adminUsersTable;
function initialize_leave_requests_table(){
  $adminUsersTable = $('#user_listing_table').DataTable({
    dom: 'Bfrtip',

    buttons: [
      'colvis','pageLength',
      {
        extend: 'excel',
        title: "Admin Users ",
        exportOptions: {
          columns: [0, 1, 2, 3]
        }
      }
    ],
    bInfo: false,
    paging: false,
    pageLength: 100,
    responsive: true,
    searching: false,
    ordering: true,
    // order: [[ 11, "desc" ]],

    // These columns are set by the function configure_data_tables in the table_settings controller
    "columnDefs": [
       { className: "text-center", "targets": [ 0, 2, 3, 4 ] },
    ],
    //scrollY: "400px",
    scrollX: true,
    scrollCollapse: true,
    fixedHeader: true,
    fixedColumns: {
      leftColumns: 2,
      //rightColumns: 2
    }
  });
}

$(document).ready(function() {
  
  // Initialize Datatables for Leave Request
  initialize_leave_requests_table();

  // Export Button Events
  $("#LEAVE_REQUESTS_TABLE_EXPORT_TO_EXCEL").on("click", function() {
    $adminUsersTable.button( '.buttons-excel' ).trigger();
  });

});