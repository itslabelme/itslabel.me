$(document).ready(function(){
  $('#country_id').change(function(){
  alert($('#country_id').val());
    $.ajax({
      url:"get_time_zone",
      type: "GET",
      dataType: 'script',
      data: {
        id: $("#country_id option:selected").val()
      },
       success: function(data){
          $("#cities_select").empty()
  .append("<%= escape_javascript(render(:partial => @time_zones)) %>")
      }
    });
  });
  });
  
 