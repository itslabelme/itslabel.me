<% @documents = Document::Base.activated.limit(5).order("created_at DESC").all %>

<div class="col-lg-12 col-md-12">
  <div class="header">
    <h2>Recent Translations</h2>
  </div>

  <!-- Rendering the Table -->
  <%= render partial: 'user/documents/index/table' %>
</div> 